```yaml
feature: mutation-git-integration
revision: 24c8659
verify_revision: fc3deaf
drift:
  - ac: mutation-git-integration#ac:git-flag-rejects-unknown
    verdict: no-drift
    narrative: "resolveGitMode rejects an unknown --git value, naming the bad value and the supported set none/stage/commit, exiting non-zero before any write"
  - ac: mutation-git-integration#ac:git-flag-default-none
    verdict: no-drift
    narrative: "with no --git flag the mode is none (no-op); written files stay untracked and no commit is created"
  - ac: mutation-git-integration#ac:git-stage-scoped
    verdict: no-drift
    narrative: "stage mode stages only the written paths via per-file go-git Add, leaving an unrelated unstaged change untouched"
  - ac: mutation-git-integration#ac:git-partial-stages-written-only
    verdict: no-drift
    narrative: "in a --continue-on-error batch only successfully-written files reach stageFiles, so the failed item contributes nothing to the index"
  - ac: mutation-git-integration#ac:git-commit-not-supported
    verdict: no-drift
    narrative: "--git=commit exits non-zero reporting commit is not yet supported, before any write, creating no commit"
  - ac: mutation-git-integration#ac:git-stage-non-repo-failloud
    verdict: no-drift
    narrative: "off a git repo, the stage preflight openRepo fails loud with a not-a-git-repository error before any file is written"
```

# Recap — mutation-git-integration @ 24c8659

Per-AC drift report comparing the `mutation-git-integration` Feature's acceptance criteria against the commits that delivered them (`Verifies:` trailers), built on the verify report at `_verify/fc3deaf.md` (6/6 pass). All 6 ACs map to one of two focused commits; every drift verdict is **no-drift**. Exit 0 (no contradictions, no errors).

## AC: git-flag-rejects-unknown

**Drift verdict:** no-drift

**Narrative:** resolveGitMode's default branch returns cli.Exit("invalid --git value %q (supported values: none, stage, commit)", 2), naming the bad value and the full supported set, non-zero, before any write — exactly what the AC requires.

**Verify verdict:** pass

**Verify justification:** resolveGitMode returns cli.Exit(invalid --git value "bogus" (supported: none, stage, commit), 2); TestEntityAdd_GitFlag_RejectsUnknown asserts the message names bogus + the set.

**Commits:**
- e76fd4b — feat(mutation-git-integration): shared --git flag + value semantics

**Evidence:**
- apps/datatugapp/commands/git_integration.go::resolveGitMode (default case)
- apps/datatugapp/commands/cmd_entity.go (resolveGitMode called before any write)

## AC: git-flag-default-none

**Drift verdict:** no-drift

**Narrative:** With no --git flag, resolveGitMode returns gitModeNone (no-op); entity add writes files and takes no VCS action, leaving them untracked and creating no commit — confirmed by TestEntityAdd_GitFlag_DefaultNone asserting "??" status, no "A " staging, and unchanged HEAD.

**Verify verdict:** pass

**Verify justification:** default mode none is a no-op; TestEntityAdd_GitFlag_DefaultNone git-inits a repo, runs add w/o --git, asserts file untracked and HEAD unchanged.

**Commits:**
- e76fd4b — feat(mutation-git-integration): shared --git flag + value semantics

**Evidence:**
- apps/datatugapp/commands/git_integration.go::resolveGitMode ("", "none" → gitModeNone)
- apps/datatugapp/commands/cmd_entity_test.go::TestEntityAdd_GitFlag_DefaultNone

## AC: git-stage-scoped

**Drift verdict:** no-drift

**Narrative:** stageFiles adds only the command's written paths per-file via go-git wt.Add (never `add -A`); test sets up an unrelated unstaged change to a tracked file, runs --git=stage, and asserts the entity file is staged while other.txt stays unstaged — exactly the AC.

**Verify verdict:** pass

**Verify justification:** stageFiles stages only written paths via go-git wt.Add (never add -A); test asserts entity file staged while unrelated change stays unstaged.

**Commits:**
- 07aa5b9 — feat(mutation-git-integration): go-git stage helper (changed-files-only)

**Evidence:**
- apps/datatugapp/commands/git_integration.go::stageFiles (per-path wt.Add(rel))
- apps/datatugapp/commands/cmd_entity_test.go::TestEntityAdd_GitStage_ScopedToWrittenFiles

## AC: git-partial-stages-written-only

**Drift verdict:** no-drift

**Narrative:** Code stages exactly the successfully-written files in a --continue-on-error batch; failed items never enter the `written` slice passed to stageFiles. Dedicated test asserts the succeeding User file staged (A) and the failing pre-existing Order file untracked/unstaged (??).

**Verify verdict:** pass

**Verify justification:** addEntitiesContinueOnError returns only actually-written paths; stageFiles stages exactly those; test asserts User staged, failed Order untracked.

**Commits:**
- 07aa5b9 — feat(mutation-git-integration): go-git stage helper (changed-files-only)

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go::addEntitiesContinueOnError (appends to `written` only on success)
- apps/datatugapp/commands/git_integration.go::stageFiles (stages only given paths)
- apps/datatugapp/commands/cmd_entity_test.go::TestEntityAdd_GitStage_PartialStagesWrittenOnly

## AC: git-commit-not-supported

**Drift verdict:** no-drift

**Narrative:** --git=commit returns cli.Exit("--git=commit is not yet supported", 2) before any write in entity add; test asserts non-zero error, no entities written, and unchanged HEAD (no commit), exactly matching the AC.

**Verify verdict:** pass

**Verify justification:** resolveGitMode returns cli.Exit(--git=commit is not yet supported, 2) before any write; test asserts the message, no entity written, HEAD unchanged.

**Commits:**
- e76fd4b — feat(mutation-git-integration): shared --git flag + value semantics

**Evidence:**
- apps/datatugapp/commands/git_integration.go::resolveGitMode ("commit" → cli.Exit(..., 2))
- apps/datatugapp/commands/cmd_entity_test.go::TestEntityAdd_GitFlag_CommitNotSupported

## AC: git-stage-non-repo-failloud

**Drift verdict:** no-drift

**Narrative:** With --git=stage off a git repo, entityAddCommandAction runs the openRepo preflight before reading input/writing; openRepo returns cli.Exit("not a git repository", 2) (non-zero), and TestEntityAdd_GitStage_NonRepoFailLoud asserts the error message plus that no entities dir is written.

**Verify verdict:** pass

**Verify justification:** gitPreflight/openRepo runs before any read/write and returns cli.Exit(... is not a git repository, 2); test asserts the error and that no entities dir was created.

**Commits:**
- 07aa5b9 — feat(mutation-git-integration): go-git stage helper (changed-files-only)

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go (gitModeStage preflight openRepo before any write)
- apps/datatugapp/commands/git_integration.go::openRepo (cli.Exit "not a git repository", 2)
- apps/datatugapp/commands/cmd_entity_test.go::TestEntityAdd_GitStage_NonRepoFailLoud

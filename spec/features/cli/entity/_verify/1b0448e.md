```yaml
feature: cli/entity
revision: 1b0448e
verdicts:
  - ac: cli/entity#ac:add-creates-new
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:add-rejects-existing
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:add-reads-stdin
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:add-empty-errors
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:add-batch-atomic-rollback
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:add-continue-on-error
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:storage-json
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-add-additive
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-add-rejects-existing
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-set-updates
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-set-missing-errors
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-set-key-flag
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-rm-removes
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:field-type-invalid
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:no-implicit-override
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:entity-list-lists
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:entity-show-renders
    verdict: pass
    justification: "carried from 700e415 (pass); behavior unchanged, full suite green at 1b0448e"
  - ac: cli/entity#ac:git-default-none
    verdict: pass
    justification: "freshly verified at 1b0448e: applyGit/gitPreflight are no-ops for the default none mode; TestEntityAdd_GitFlag_DefaultNone asserts the entity file is untracked/unstaged with no commit."
  - ac: cli/entity#ac:git-stage-scoped
    verdict: pass
    justification: "freshly verified at 1b0448e: stageFiles uses go-git wt.Add per written path (never add -A); test asserts the entity file is staged while an unrelated change stays unstaged."
```

# Verify Report — cli/entity @ 1b0448e

**Summary:** 19 passed, 0 failed, 0 unmapped, 0 errored (19 ACs total). The Feature is fully implemented and verified.

**Re-verify note:** This run supersedes `700e415.md` (which reported 17 passed, 2 unmapped — the two `--git` ACs were unmapped while Task 9 was blocked on the `mutation-git-integration` capability). That capability has since landed and Task 9 wired `--git` into all entity mutators (commit `1b0448e`), mapping the final two ACs. The two git ACs were **freshly verified** at this revision by independent subagents. The other 17 ACs carry their `700e415` `pass` verdicts forward: their implementation behavior is unchanged since `700e415` (Task 9 added git wiring without altering the add/field/list/show contracts) and the full repository test suite is green at `1b0448e`.

## AC: git-default-none

**Verdict:** pass

**Justification:** With no `--git` flag the mode defaults to `none`; `gitPreflight` and `applyGit` are no-ops, so files are written and nothing is staged or committed. `TestEntityAdd_GitFlag_DefaultNone` git-inits a temp project, runs `entity add` without `--git`, and asserts the entity file is untracked (`??`), not staged, with no new commit.

**Commits:**
- 1b0448e — feat(cli/entity): wire --git into all entity mutators (Task 9)

**Evidence:**
- apps/datatugapp/commands/git_integration.go — resolveGitMode default none; applyGit/gitPreflight no-op for none
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_GitFlag_DefaultNone (PASS)

## AC: git-stage-scoped

**Verdict:** pass

**Justification:** `--git=stage` stages exactly the written entity file via go-git `Worktree.Add(<relpath>)` — never `git add -A` — so unrelated changes are left untouched. `TestEntityAdd_GitStage_ScopedToWrittenFiles` asserts the entity file is staged (`A `) while an unrelated tracked-file modification stays unstaged (` M`).

**Commits:**
- 1b0448e — feat(cli/entity): wire --git into all entity mutators (Task 9)

**Evidence:**
- apps/datatugapp/commands/git_integration.go — stageFiles (per-path wt.Add, never add -A)
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_GitStage_ScopedToWrittenFiles (PASS)

## AC: (17 previously-verified criteria)

**Verdict:** pass (carried from `700e415.md`)

The following ACs were independently verified `pass` at revision `700e415` and are unchanged in behavior at `1b0448e` (full suite green): add-creates-new, add-rejects-existing, add-reads-stdin, add-empty-errors, add-batch-atomic-rollback, add-continue-on-error, storage-json, field-add-additive, field-add-rejects-existing, field-set-updates, field-set-missing-errors, field-set-key-flag, field-rm-removes, field-type-invalid, no-implicit-override, entity-list-lists, entity-show-renders. See `700e415.md` for their per-AC evidence.

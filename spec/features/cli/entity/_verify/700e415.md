```yaml
feature: cli/entity
revision: 700e415
verdicts:
  - ac: cli/entity#ac:add-creates-new
    verdict: pass
    justification: "entityAddCommandAction parses doc, create-only guard, SaveEntity creates User, returns nil (exit 0); TestEntityAdd_CreatesNew_StorageJSON passes."
  - ac: cli/entity#ac:add-rejects-existing
    verdict: pass
    justification: "create-only guard returns non-zero naming User unless LoadEntity is os.ErrNotExist; SaveEntity skipped so file untouched; rejection tests pass."
  - ac: cli/entity#ac:add-reads-stdin
    verdict: pass
    justification: "reads stdin via c.Root().Reader when -f absent/'-'; TestEntityAdd_ReadsStdin asserts entity created."
  - ac: cli/entity#ac:add-empty-errors
    verdict: pass
    justification: "empty/whitespace input -> cli.Exit('empty input',2) before any write; test asserts non-zero + entities dir not created."
  - ac: cli/entity#ac:add-batch-atomic-rollback
    verdict: pass
    justification: "addEntitiesAtomic preflights all; any conflict skips atomicWriteFiles (writes nothing), reports 'failed: Order (already exists)', returns non-zero; test passes."
  - ac: cli/entity#ac:add-continue-on-error
    verdict: pass
    justification: "addEntitiesContinueOnError writes User, reports Order failed, returns non-zero; TestEntityAdd_BatchContinueOnError passes."
  - ac: cli/entity#ac:storage-json
    verdict: pass
    justification: "YAML def stored as canonical JSON at nested entities/User/User.entity.json; test asserts FileExists + json.Valid."
  - ac: cli/entity#ac:field-add-additive
    verdict: pass
    justification: "field add appends primaryCurrency, leaves id unchanged; TestEntityFieldAdd_Additive passes."
  - ac: cli/entity#ac:field-add-rejects-existing
    verdict: pass
    justification: "atomic collision on existing field id returns non-zero, writes nothing; test asserts error + before==after."
  - ac: cli/entity#ac:field-set-updates
    verdict: pass
    justification: "field set User primaryCurrency --type currency sets type and exits 0 at HEAD (currency now a known type); test passes."
  - ac: cli/entity#ac:field-set-missing-errors
    verdict: pass
    justification: "absent field -> cli.Exit('field not found',1) before write; test asserts error + byte-identical entity file."
  - ac: cli/entity#ac:field-set-key-flag
    verdict: pass
    justification: "--key sets IsKeyField=true, returns nil (exit 0); TestEntityFieldSet_KeyFlag passes."
  - ac: cli/entity#ac:field-rm-removes
    verdict: pass
    justification: "rm removes field and writes; second rm of absent field returns cli.Exit(...,1) non-zero; test passes."
  - ac: cli/entity#ac:field-type-invalid
    verdict: pass
    justification: "validateFieldType rejects non-empty unknown 'not-a-type' across mutation paths, writes nothing; TestEntityAdd_InvalidFieldType_Errors passes."
  - ac: cli/entity#ac:no-implicit-override
    verdict: pass
    justification: "colliding id (string vs integer) fails non-zero before any write; on-disk id stays integer; TestEntityFieldAdd_NoImplicitOverride passes."
  - ac: cli/entity#ac:entity-list-lists
    verdict: pass
    justification: "entity list prints every LoadEntities item; TestEntityList_Lists asserts User and Order appear."
  - ac: cli/entity#ac:entity-show-renders
    verdict: pass
    justification: "entity show renders fields plus labelled read-only generated mapping copy (tables), never writes; test asserts field+table appear and file byte-identical."
  - ac: cli/entity#ac:git-default-none
    verdict: unmapped
    justification: "no commits reference this AC (Task 9 blocked on mutation-git-integration)"
  - ac: cli/entity#ac:git-stage-scoped
    verdict: unmapped
    justification: "no commits reference this AC (Task 9 blocked on mutation-git-integration)"
```

# Verify Report — cli/entity @ 700e415

**Summary:** 17 passed, 0 failed, 2 unmapped, 0 errored (19 ACs total). The two `unmapped` ACs are the `--git` integration criteria, deferred with Task 9 (blocked on the cross-repo `mutation-git-integration` capability).

## AC: add-creates-new

**Verdict:** pass

**Justification:** `entityAddCommandAction` parses the YAML/JSON doc, applies the create-only guard, persists the entity and returns nil (exit 0).

**Commits:**
- 2fb244c — feat(cli/entity): add create-only `entity add` with nested JSON storage

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_CreatesNew_StorageJSON (PASS)

## AC: add-rejects-existing

**Verdict:** pass

**Justification:** The create-only guard returns a non-zero error naming `User` unless `LoadEntity` yields `os.ErrNotExist`; the existing file is never re-written.

**Commits:**
- 2fb244c — feat(cli/entity): add create-only `entity add` with nested JSON storage

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — create-only guard
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_RejectsExisting, TestEntityAdd_RejectsExisting_CorruptFile (PASS)

## AC: add-reads-stdin

**Verdict:** pass

**Justification:** Input is read from stdin via `c.Root().Reader` when `-f` is absent or `-`; the entity is created.

**Commits:**
- b10b9b2 — feat(cli/entity): read `entity add` input from stdin + --format + empty-input guard

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — stdin source resolution
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_ReadsStdin (PASS)

## AC: add-empty-errors

**Verdict:** pass

**Justification:** A whitespace-only/empty input triggers `cli.Exit("empty input", 2)` before any parse or write.

**Commits:**
- b10b9b2 — feat(cli/entity): read `entity add` input from stdin + --format + empty-input guard

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — empty-input guard
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_EmptyStdin_Errors (PASS)

## AC: add-batch-atomic-rollback

**Verdict:** pass

**Justification:** `addEntitiesAtomic` preflights all items; any conflict skips `atomicWriteFiles` (nothing written), reports `failed: Order (already exists)`, and returns non-zero.

**Commits:**
- edd705c — feat(cli/entity): batch `entity add` with atomic commit + --continue-on-error

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — addEntitiesAtomic
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_BatchAtomicRollback (PASS)

## AC: add-continue-on-error

**Verdict:** pass

**Justification:** `addEntitiesContinueOnError` writes the passing `User`, reports `Order` as failed, and returns non-zero.

**Commits:**
- edd705c — feat(cli/entity): batch `entity add` with atomic commit + --continue-on-error

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — addEntitiesContinueOnError
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_BatchContinueOnError (PASS)

## AC: storage-json

**Verdict:** pass

**Justification:** A YAML definition is persisted as canonical JSON at the nested path `entities/User/User.entity.json`.

**Commits:**
- 2fb244c — feat(cli/entity): add create-only `entity add` with nested JSON storage

**Evidence:**
- pkg/datatug-core/storage/filestore/store_entities.go — nested layout
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_CreatesNew_StorageJSON (FileExists + json.Valid) (PASS)

## AC: field-add-additive

**Verdict:** pass

**Justification:** `field add` appends `primaryCurrency` while leaving `id` unchanged.

**Commits:**
- 6f51273 — feat(cli/entity): add additive batch `entity field add`

**Evidence:**
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldAdd_Additive (PASS)

## AC: field-add-rejects-existing

**Verdict:** pass

**Justification:** An atomic collision on the existing field `id` returns non-zero and writes nothing (entity unchanged).

**Commits:**
- 6f51273 — feat(cli/entity): add additive batch `entity field add`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — atomic collision check
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldAdd_RejectsExisting (PASS)

## AC: field-set-updates

**Verdict:** pass

**Justification:** `field set User primaryCurrency --type currency` sets the type and exits 0 at HEAD (`currency` registered as a known type in 26f34d4).

**Commits:**
- 69e0280 — feat(cli/entity): add surgical `entity field set`
- 26f34d4 — feat(cli/entity): reject unknown field types across mutation paths (registers `currency`)

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — field set + validateFieldType
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldSet_UpdatesType (PASS)

## AC: field-set-missing-errors

**Verdict:** pass

**Justification:** Setting an absent field returns `cli.Exit("field not found", 1)` before any write; the entity file is byte-identical.

**Commits:**
- 69e0280 — feat(cli/entity): add surgical `entity field set`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — field-not-found guard
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldSet_MissingErrors (PASS)

## AC: field-set-key-flag

**Verdict:** pass

**Justification:** `--key` sets `IsKeyField=true` and the command exits 0.

**Commits:**
- 69e0280 — feat(cli/entity): add surgical `entity field set`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — --key handling
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldSet_KeyFlag (PASS)

## AC: field-rm-removes

**Verdict:** pass

**Justification:** `rm` removes the field and writes; a second `rm` of the now-absent field returns `cli.Exit(...,1)` (non-zero) before any write.

**Commits:**
- 090536e — feat(cli/entity): add `entity field rm`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — entityFieldRmCommandAction
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldRm_Removes (PASS)

## AC: field-type-invalid

**Verdict:** pass

**Justification:** `validateFieldType` rejects a non-empty unknown type (`not-a-type`) across add/field-add/field-set, surfacing the unknown type and writing nothing.

**Commits:**
- 26f34d4 — feat(cli/entity): reject unknown field types across mutation paths

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — validateFieldType / validateEntityFieldTypes
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityAdd_InvalidFieldType_Errors (PASS)

## AC: no-implicit-override

**Verdict:** pass

**Justification:** Adding a colliding `id` (string vs integer) fails non-zero before any write; the on-disk `id` stays typed `integer`.

**Commits:**
- 6f51273 — feat(cli/entity): add additive batch `entity field add`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — atomic collision check
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityFieldAdd_NoImplicitOverride (PASS)

## AC: entity-list-lists

**Verdict:** pass

**Justification:** `entity list` prints every entity from `LoadEntities`; both `User` and `Order` appear.

**Commits:**
- 1c4b08e — feat(cli/entity): add read-back `entity list` and `entity show`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — entityListCommandAction
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityList_Lists (PASS)

## AC: entity-show-renders

**Verdict:** pass

**Justification:** `entity show` renders the fields plus a clearly labelled read-only generated mapping copy (tables) and never writes; the on-disk file is byte-identical before/after.

**Commits:**
- 1c4b08e — feat(cli/entity): add read-back `entity list` and `entity show`

**Evidence:**
- apps/datatugapp/commands/cmd_entity.go — entityShowCommandAction / renderEntityShow
- apps/datatugapp/commands/cmd_entity_test.go — TestEntityShow_Renders (PASS)

## AC: git-default-none

**Verdict:** unmapped

**Justification:** No commits reference this AC. Task 9 (`--git` integration) is blocked on the cross-repo `mutation-git-integration` capability, which has not landed in this repo.

**Commits:**
- No commits reference this AC.

**Evidence:**
- spec/plans/2026-06-04-cli-entity.md — Task 9 (Status: blocked)

## AC: git-stage-scoped

**Verdict:** unmapped

**Justification:** No commits reference this AC. Task 9 (`--git` integration) is blocked on the cross-repo `mutation-git-integration` capability, which has not landed in this repo.

**Commits:**
- No commits reference this AC.

**Evidence:**
- spec/plans/2026-06-04-cli-entity.md — Task 9 (Status: blocked)
